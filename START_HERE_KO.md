# InstaSave — HTML / CSS / JavaScript 버전

React, Next.js, TypeScript, Tailwind 없이 구성한 수정용 전체 소스입니다.
기존 노란 배경, 카드 디자인, 반응형, 사진·릴스 미리보기, 개별 다운로드, ZIP 다운로드를 옮겼습니다.
별도 소스 버전이며 기존 배포 사이트를 변경하지 않습니다.

## 실행하기

1. Node.js 22 이상을 설치합니다.
2. 압축을 풀고 `instasave-vanilla` 폴더를 안티그래비티에서 엽니다.
3. 터미널에서 `npm start`를 실행합니다. `npm install`이나 빌드는 필요 없습니다.
4. 브라우저에서 http://localhost:3000 을 엽니다.
5. 종료하려면 터미널에서 Ctrl+C를 누릅니다.

HTML을 더블클릭하면 디자인은 볼 수 있지만 모듈 스크립트와 추출 API가 동작하지 않습니다.
실제 기능 확인은 반드시 위 서버로 실행하세요. 정적 Live Server만으로는 추출 기능을 사용할 수 없습니다.
화면/CSS/JS 수정 후 브라우저를 새로고침하세요. 서버 수정 중에는 `npm run dev`로 실행하면 자동 재시작됩니다.

## 어디를 수정하나요?

| 파일 | 수정 내용 |
|---|---|
| index.html | 화면 구조, 제목, 설명, 버튼 문구, 배경 오브제 |
| style.css | 색상, 크기, 여백, 반응형, 호버·애니메이션 |
| main.js | 버튼 동작, 결과 카드, 초기화, 다운로드 |
| server.js | 로컬 웹서버, API 연결 |
| api/extract.js | 게시물 분석 요청·오류 응답 |
| api/media.js | 이미지·MP4 전달, 영상 구간 요청 |
| lib/instagram.js | Instagram 게시물·릴스·캐러셀 분석 |
| lib/instagram-url.js | 게시물 URL 검증, 미디어 주소 허용 목록 |
| lib/zip.js | 전체 다운로드 ZIP 생성 |
| tests/ | 자동 검증 |

## 안티그래비티에 입력할 예시

```text
현재 프로젝트는 React 없는 HTML + CSS + Vanilla JavaScript 프로젝트다.
index.html은 화면 구조, style.css는 디자인, main.js는 화면 동작이다.
기존 사진·릴스 추출 API와 개별/ZIP 다운로드 기능을 유지하면서 아래 내용을 수정해줘.
React, Next.js, TypeScript로 전환하지 말고 현재 파일 구조를 유지해줘.
실행은 npm start 후 http://localhost:3000에서 확인해줘.

수정할 내용:
[원하는 변경 사항 입력]
```

## 기능과 제한

서버에서 Instagram 공개 페이지와 공개 메타데이터를 분석합니다.
캐러셀의 사진·영상 순서를 유지하고 MP4 주소가 제공되는 릴스를 지원합니다.
Instagram CDN 미디어를 같은 서버에서 전달하여 브라우저의 직접 접근 문제를 줄입니다.
비공개·삭제·로그인 요구·접근 제한 게시물은 실패할 수 있습니다. Instagram 구조 변경 시 분석 코드 수정이 필요합니다.
사진 1개 15MB, 영상 1개 80MB, 전체 ZIP의 원본 합계 80MB 제한입니다.
클립보드 읽기는 브라우저 권한과 HTTPS 또는 localhost 환경이 필요합니다.

외부 서비스 패키지 없이 Node.js 기본 모듈만 사용합니다.
서버는 로컬 수정용으로 127.0.0.1에 연결합니다. 공개 배포 시에는 Node.js 지원 호스팅,
HTTPS 프록시 설정과 동일 출처 확인 방식을 배포 환경에 맞춰 구성해야 합니다.
정적 파일 전용 호스팅에 올리면 추출 API가 실행되지 않습니다.

## 검증

`npm test`로 URL 검증, 캐러셀, 릴스 MP4, ZIP, 미디어 프록시 및 독립 서버 연결을 검사합니다.
글꼴 변환 추가 버전의 26개 자동 테스트가 통과했습니다. 외부 응답은 테스트용 데이터로 검증합니다.
이 변환 작업에서는 실제 Instagram 게시물의 온라인 추출과 브라우저 시각 검증을 다시 수행하지 않았습니다.


## 추가된 글꼴 변환 기능

상단의 `Aa 글꼴 변환` 탭에서 문구를 입력하면 72가지 유니코드 스타일을 표시합니다.
각 카드의 복사하기 버튼으로 복사합니다. 브라우저가 자동 복사를 제한하면 직접 복사 입력창을 표시합니다.
한글, 이모지, 공백, 줄바꿈은 유지하며 영문과 지원되는 숫자를 변환합니다.
빈 입력일 때는 예시만 표시하고 복사 버튼은 비활성화합니다. 최대 입력은 HTML maxlength 기준 500 UTF-16 코드 단위입니다.
탭을 바꿔도 입력과 미디어 결과가 유지됩니다. 초기화 버튼은 현재 탭만 초기화합니다.

- `lib/fonts.js`: 72가지 문자 매핑과 변환 함수
- `fonts-ui.js`: 탭, 실시간 미리보기, 복사·수동 복사, 입력 초기화
- `index.html`: 탭과 글꼴 화면
- `style.css` 하단: 글꼴 화면 전용 스타일
- `server.js`: 새 JS 파일 제공 경로 추가

기존 추출 API와 다운로드 코드는 유지했습니다. 빈 HTTP 404/405 응답을 JSON으로 읽다가 발생하던 오류에는 서버 실행 안내를 추가했습니다.
미디어 추출은 `npm start` 후 http://localhost:3000 에서 사용하세요. Live Server 5500 포트에는 추출 서버 기능이 없습니다.
글꼴 변환 자체는 브라우저에서 작동하며 외부 서비스에 입력 문구를 전송하지 않습니다.

검증: 문자 매핑, 유니코드 예외 문자, 한글·이모지 보존 및 기존 API/서버를 포함한 자동 테스트 26개 통과.
이번 환경에 브라우저 실행 파일이 없어 실제 화면·클립보드 동작은 브라우저에서 검증하지 못했습니다.


### 글꼴 확장 업데이트

총 72종: 문자 스타일 36종, 선·기호 장식 12종, 문구 프레임 24종.
전체/문자 스타일/선·기호 장식/문구 프레임 필터로 선택할 수 있습니다.
스몰캡, 위첨자, 아래첨자 등은 대응하는 유니코드가 없는 글자를 원문으로 유지합니다.
프레임은 한글에도 적용되며, 빈 줄은 그대로 유지합니다. 결합 장식은 영문·숫자에만 적용합니다.
참고 사이트의 모든 스타일을 복제한 것이 아니라 지원 가능한 변환과 장식을 추가한 버전입니다.


### 문자 스타일만 확장

기존 문자 스타일 24종을 그대로 유지하고 12종을 추가했습니다(문자 스타일 총 36종).
추가: 후크, 스트로크, 커브, 와이드 스몰캡, 포네틱, 앵글 레터 6종 및 교차 혼합 서체 6종.
유사 모양 문자를 활용한 스타일과 두 기존 서체를 영문 순서대로 교차하는 혼합 스타일입니다.
새 스타일은 숫자·한글·이모지를 원문으로 유지합니다. 선·기호 장식 12종과 문구 프레임 24종은 변경하지 않았습니다.
기존 60종의 출력 보존 비교와 자동 테스트 26개를 통과했습니다. 브라우저 시각 검증은 수행하지 못했습니다.


### ChatGPT 바로가기
상단 헤더에 ChatGPT 버튼을 추가했습니다. https://chatgpt.com/ko-KR/ 링크가 새 탭에서 열립니다. 작은 모바일 화면에서는 버튼과 초기화가 로고 아래 줄에 배치됩니다.


## 글꼴 변환 탭의 감성 문구 바로가기

‘변환할 문구’ 입력창 위의 보라색 ‘ChatGPT에서 감성 문구 만들기’ 버튼을 누르면 지정된 ChatGPT 대화가 새 탭에서 열립니다.
ChatGPT에서 만든 문구를 복사한 뒤 이 사이트의 입력창에 붙여넣어 글꼴을 변환하세요.
대화 링크는 해당 ChatGPT 계정의 로그인 및 접근 권한이 필요할 수 있습니다.
기존 AI 소개 생성 UI와 서버 API는 제거했으며 OpenAI API 키 설정은 필요하지 않습니다.
버튼의 문구와 링크는 index.html, 디자인은 style.css의 .font-chatgpt-shortcut에서 수정할 수 있습니다.
